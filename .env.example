# Variables de entorno necesarias (ejemplo)
AUTH_SECRET=your_secret_here

# Cómo usar:
# 1) Copiá este archivo a .env.local
#    cp .env.example .env.local
# 2) Editá .env.local y poné el valor real
# 3) .env/.env.local NO se suben al repo (están en .gitignore)
